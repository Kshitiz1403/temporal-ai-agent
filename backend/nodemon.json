{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node -r dotenv/config -r tsconfig-paths/register --transpile-only ./src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
